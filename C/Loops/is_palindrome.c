#include <stdio.h>

int main()
{
    char sentence[] = "";
    scanf("%s", &sentence);
    printf("%s\n", sentence);

    //for (int j; in [a]; j++)
    {

    }

    //if ()
    {
        // is palindrome
    }
    // is not a palindrome

    return 0;
}
